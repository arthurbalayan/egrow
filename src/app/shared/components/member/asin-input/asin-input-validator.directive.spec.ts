import {AsinInputValidatorDirective} from './asin-input-validator.directive';

describe('AsinInputValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AsinInputValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
