/**
 * Represents the structure of the statistics which are displayed
 * in the statistics component.
 */
export interface Statistics {
    numProjects: number;
    minRanking: number;
    avgRanking: number;
    maxRanking: number;
}
