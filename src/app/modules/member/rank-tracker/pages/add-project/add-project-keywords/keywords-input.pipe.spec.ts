import {KeywordsInputPipe} from './keywords-input.pipe';

describe('KeywordsInputPipe', () => {
  it('create an instance', () => {
    const pipe = new KeywordsInputPipe();
    expect(pipe).toBeTruthy();
  });
});
